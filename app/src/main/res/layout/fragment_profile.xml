<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:padding="16dp">

    <TextView
        android:id="@+id/displayNameTextView"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="Full Name"
        android:textSize="18sp"
        android:textStyle="bold"
        android:layout_marginTop="16dp" />

    <TextView
        android:id="@+id/displayEmailTextView"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_below="@id/displayNameTextView"
        android:text="Email"
        android:textSize="16sp"
        android:layout_marginTop="8dp" />

    <!-- Editable Name -->
    <EditText
        android:id="@+id/editNameEditText"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:hint="Enter new name"
        android:layout_below="@id/displayEmailTextView"
        android:layout_marginTop="16dp"
        android:visibility="gone" />

    <!-- Skills Field -->
    <EditText
        android:id="@+id/editSkillsEditText"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:hint="Enter skills"
        android:layout_below="@id/editNameEditText"
        android:layout_marginTop="8dp"
        android:visibility="gone" />

    <!-- Experience Field -->
    <EditText
        android:id="@+id/editExperienceEditText"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:hint="Enter experience"
        android:layout_below="@id/editSkillsEditText"
        android:layout_marginTop="8dp"
        android:visibility="gone" />

    <!-- Education Field -->
    <EditText
        android:id="@+id/editEducationEditText"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:hint="Enter education"
        android:layout_below="@id/editExperienceEditText"
        android:layout_marginTop="8dp"
        android:visibility="gone" />

    <!-- Edit, Save, Reset Password, and Logout Buttons -->
    <Button
        android:id="@+id/editButton"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="Edit Profile"
        android:layout_below="@id/editEducationEditText"
        android:layout_marginTop="16dp" />

    <Button
        android:id="@+id/saveButton"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="Save Changes"
        android:layout_below="@id/editButton"
        android:layout_marginTop="16dp"
        android:visibility="gone" />

    <Button
        android:id="@+id/resetPasswordButton"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="Reset Password"
        android:layout_below="@id/saveButton"
        android:layout_marginTop="16dp" />

    <Button
        android:id="@+id/logoutButton"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="Logout"
        android:layout_below="@id/resetPasswordButton"
        android:layout_marginTop="16dp" />
</RelativeLayout>


